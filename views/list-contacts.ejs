<!DOCTYPE html>
<html>
  <head>
    <title>Survey Service: Contacts</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <a href="/"><h1>Survey Service</h1></a>
    <% if (items.length > 0) { %>
    <ul>
      <% items.forEach(function(item) { %>
      <li><a href="/contacts/<%=item._id%>"><%=item.name%></a></li>
      <% }); %>
    </ul>
    <% } else { %>
    <div>No contacts found.</div>
    <% } %>
    <div class="control">
      <h2>Search for contacts by user name</h2>
      <form method="get" action="/contacts/">
        <label>User name: <input name="name"></label>
        <button>Search</button>
      </form>
    </div>
  </body>
</html>