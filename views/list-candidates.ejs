<!DOCTYPE html>
<html>
  <head>
    <title>Survey Manager: Respondents</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <a href="/"><h1>Survey Manager</h1></a>
    <% if (items.length > 0) { %>
    <ul>
      <% items.forEach(function(item) { %>
      <li><a href="/respondents/<%=item._id%>"><%=item.name%></a></li>
      <% }); %>
    </ul>
    <% } else { %>
    <div>No respondents found.</div>
    <% } %>
    <div class="control">
      <h2>Search for respondents by name</h2>
      <form method="get" action="/respondents/">
        <label>Respondent name: <input name="name"></label>
        <button>Search</button>
      </form>
    </div>
  </body>
</html>
